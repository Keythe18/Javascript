/** DOM => Document Object Model / Document Object Manager
 * 
 * 1 - Cibler l'element sur lequel on va faire la modification
 * 2 - Effectuer la modification
 * 
 * Selecteurs =>
 * 
 * document.getElementById() // recupere un seul element avec un identifiant
 * document.getElementsByClassName() //  recupere tous les elements ayant la meme classe (dans un tableau)
 * document.getElementsByTagName() // recupere tous les elements ayant le meme nom d'element (dans un tableau)
 * 
 * document.querySelector('#myId') // recupere le premier element ayant un tel critere (identifiant)
 * document.querySelector('.maclasse') // recupere le premier element ayant un tel critere (classe)
 * document.querySelector('div') // recupere le premier element ayant un tel critere (tag name (le nom de la balise || le nom de l'element))
 * 
 * document.querySelectorAll('') // recupere tous les elements avec un tel critere (balise, classe, id)
 * 
 * 
 * 
 * 
 * events
 * https://developer.mozilla.org/fr/docs/Web/Events
 * 
 * element.addEventListener("une chaine (le nom de l'evenement)", actionAExecuter)
 * element.removeEventListener("une chaine (le nom de l'evenement)", actionAExecuter)
*/
